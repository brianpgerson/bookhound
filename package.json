{
  "name": "bookhound",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {},
  "description": "This example is so cool.",
  "license": "MIT",
  "engines": {
    "node": "6.9.5"
  },
  "dependencies": {
    "concurrently": "^3.1.0",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "build": "concurrently \"cd client && npm install -g --save\" \"cd server && npm install -g --save\"",
    "clean": "concurrently \"rimraf node_modules\" \"cd client && rimraf node_modules build\" \"cd server && rimraf node_modules build\"",
    "heroku-postbuild": "npm install",
    "build-client": "cd client && npm install -g",
	"build-server": "cd server && npm install -g",
    "start": "concurrently \"cd client && npm start\" \"cd server && npm start\"",
    "start:prod": "cd server && npm start:prod"
  }
}